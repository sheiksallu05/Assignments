import Child from "./Child";

function Parent() {

    return (
        <div>
            <h2>Parent Component</h2>

            <p>This is the parent component.</p>

            <Child />
        </div>
    );
}

export default Parent;